# 盼趣 AI 前端样式规范

> 基于 index.html、admin-dashboard.html 和 Projects.html 生成的统一前端规范
> 最后更新: 2026-01-20

> **核心原则: 严格遵守 DaisyUI 组件标准和 HTML 结构**
> 所有页面必须使用 DaisyUI 的标准组件类和正确的 HTML 结构

---

## 目录

1. [技术栈](#技术栈)
2. [DaisyUI 核心原则](#daisyui-核心原则)
3. [颜色系统](#颜色系统)
4. [字体系统](#字体系统)
5. [布局系统](#布局系统)
6. [组件规范](#组件规范)
7. [动画效果](#动画效果)
8. [主题切换](#主题切换)
9. [响应式设计](#响应式设计)
10. [代码组织](#代码组织)

---

## 技术栈

### 核心框架
- **Tailwind CSS** - 实用工具优先的 CSS 框架
- **DaisyUI v5+** - 基于 Tailwind 的组件库 (最新版本)
- **RemixIcon v3.5.0** - 图标库

### 字体
- **Inter** - 主要字体 (Google Fonts)
  - 可用字重: 300, 400, 500, 600, 700

### 图表库
- **Chart.js v4.4.1** - 数据可视化

### CDN 配置
```html
<!-- Tailwind CSS + DaisyUI (使用最新版本) -->
<link href="https://cdn.jsdelivr.net/npm/daisyui@latest/dist/full.min.css" rel="stylesheet" type="text/css" />
<script src="https://cdn.tailwindcss.com"></script>

<!-- Icons -->
<link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">

<!-- Fonts -->
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
```

---

## DaisyUI 核心原则

### ⚠️ 必须遵守的规则

#### 1. 使用 DaisyUI 标准组件类

**❌ 错误示例 - 自定义布局**
```html
<div class="flex items-center justify-between">
  <div>左侧内容</div>
  <div>右侧内容</div>
</div>
```

**✅ 正确示例 - DaisyUI Navbar**
```html
<div class="navbar">
  <div class="navbar-start">
    <a href="#" class="btn btn-ghost">Logo</a>
  </div>
  <div class="navbar-end">
    <div class="dropdown dropdown-end">
      <button class="btn btn-ghost">菜单</button>
    </div>
  </div>
</div>
```

#### 2. 遵循 DaisyUI HTML 结构

每个 DaisyUI 组件都有标准的 HTML 结构：

**Navbar 标准结构**
```html
<div class="navbar">
  <div class="navbar-start">...</div>
  <div class="navbar-center">...</div>
  <div class="navbar-end">...</div>
</div>
```

**Menu 标准结构**
```html
<ul class="menu">
  <li><a>菜单项</a></li>
</ul>
```

**Card 标准结构**
```html
<div class="card">
  <figure><img src="..." /></figure>
  <div class="card-body">
    <h3 class="card-title">标题</h3>
    <div class="card-actions">...</div>
  </div>
</div>
```

**Dropdown 标准结构**
```html
<div class="dropdown dropdown-end">
  <div tabindex="0" role="button" class="btn">触发器</div>
  <ul tabindex="0" class="dropdown-content menu p-2 shadow bg-base-100 rounded-box">
    <li><a>选项</a></li>
  </ul>
</div>
```

#### 3. 不要在自定义 CSS 中覆盖 DaisyUI 样式

**❌ 错误**
```css
.navbar-start {
  display: flex;  /* DaisyUI 已定义，不要覆盖 */
  align-items: center;
}
```

**✅ 正确**
```css
/* 只在需要额外增强时使用自定义类 */
.navbar-with-sidebar .navbar-start {
  /* 仅添加项目特定的样式 */
}
```

#### 4. 使用 DaisyUI 变量而非硬编码

**❌ 错误**
```html
<div style="background-color: #002FA7;">按钮</div>
```

**✅ 正确**
```html
<div class="bg-primary">按钮</div>
```

#### 5. 自定义类的使用规范

**核心原则: 95% DaisyUI 标准类 + 5% 自定义类**

自定义类**仅限**用于以下场景:
1. **渐变效果** - DaisyUI 不支持的渐变样式
2. **特殊动画** - 复杂的 keyframe 动画
3. **项目特定增强** - 在不破坏 DaisyUI 结构的前提下添加项目特定样式

**允许的自定义类示例:**

```css
/* ✅ 允许 - 渐变文本 */
.gradient-text {
  background: linear-gradient(135deg, #002FA7 0%, #2560FF 50%, #4D7CFF 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* ✅ 允许 - 项目特定导航栏增强 */
.navbar-with-sidebar {
  /* 仅添加项目特定的样式，不覆盖 DaisyUI 默认样式 */
}

/* ✅ 允许 - 自定义动画 */
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}
.fade-in {
  animation: fadeIn 0.5s ease-out;
}

/* ✅ 允许 - 卡片悬停增强 */
.card-hover:hover {
  transform: translateY(-8px);
  box-shadow: 0 25px 50px -12px rgba(0, 47, 167, 0.25);
}
```

**禁止的自定义类示例:**

```css
/* ❌ 禁止 - 覆盖 DaisyUI 核心样式 */
.navbar {
  display: flex; /* DaisyUI 已定义 */
  padding: 1rem; /* 应使用 Tailwind 类 */
}

/* ❌ 禁止 - 重新实现 DaisyUI 组件 */
.custom-card {
  background: white;
  border-radius: 0.5rem;
  padding: 1.5rem;
  /* 应使用 .card .card-body */
}

/* ❌ 禁止 - 可以用 Tailwind 类实现的样式 */
.flex-center {
  display: flex;
  align-items: center;
  justify-content: center;
  /* 应使用 flex items-center justify-center */
}
```

#### 6. 项目文件结构规范

**自定义样式文件位置:**

```
panqu-web/
├── assets/
│   ├── css/
│   │   ├── project/
│   │   │   ├── project-config.js    # Tailwind 配置
│   │   │   └── project-styles.css   # 项目特定样式
│   │   ├── billing/
│   │   │   └── billing-common.css   # 账单页面样式
│   │   └── shared/
│   │       └── styles.css           # 共享样式
│   └── js/
│       └── project/
│           └── project-config.js    # 可选的 JS 配置
```

**样式文件引入顺序:**

```html
<head>
  <!-- 1. DaisyUI + Tailwind -->
  <link href="https://cdn.jsdelivr.net/npm/daisyui@latest/dist/full.min.css" rel="stylesheet" type="text/css" />
  <script src="https://cdn.tailwindcss.com"></script>
  
  <!-- 2. Tailwind 配置 (如果需要) -->
  <script src="../assets/js/project/project-config.js"></script>
  
  <!-- 3. 项目特定样式 (最后加载) -->
  <link rel="stylesheet" href="../assets/css/project/project-styles.css">
</head>
```

**project-styles.css 内容结构:**

```css
/* ============================================ */
/* 渐变效果                                     */
/* ============================================ */

.gradient-text {
  background: linear-gradient(135deg, #002FA7 0%, #2560FF 50%, #4D7CFF 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* ============================================ */
/* 动画效果                                     */
/* ============================================ */

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

.fade-in {
  animation: fadeIn 0.5s ease-out;
}

/* ============================================ */
/* 项目特定增强 (不覆盖 DaisyUI 核心)          */
/* ============================================ */

/* 导航栏增强 - 仅添加项目特定样式 */
.navbar-with-sidebar .swap i {
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 卡片悬停增强 */
.card-hover:hover {
  transform: translateY(-8px);
  box-shadow: 0 25px 50px -12px rgba(0, 47, 167, 0.25);
}
```

**关键要点:**
- ✅ 所有自定义样式必须放在 `assets/css/` 目录下
- ✅ 按功能模块组织样式文件 (project/, billing/, shared/)
- ✅ 样式文件内使用注释分隔不同功能区域
- ✅ 自定义类名使用 kebab-case 命名
- ✅ 在 HTML 中最后引入自定义样式
- ❌ 不要在 `<style>` 标签中写大量自定义样式
- ❌ 不要创建与 DaisyUI 组件同名的自定义类

### DaisyUI 常用组件速查

| 组件 | 标准类 | 关键子元素 |
|------|--------|-----------|
| Navbar | `.navbar` | `.navbar-start`, `.navbar-center`, `.navbar-end` |
| Menu | `.menu` | `.menu-horizontal`, `.menu-vertical`, `.menu-title` |
| Button | `.btn` | `.btn-primary`, `.btn-ghost`, `.btn-outline`, `.btn-circle`, `.btn-square` |
| Card | `.card` | `.card-body`, `.card-title`, `.card-actions`, `figure` |
| Dropdown | `.dropdown` | `.dropdown-end`, `.dropdown-content` |
| Form | `.form-control` | `.input`, `.textarea`, `.select`, `.label`, `.label-text` |
| Badge | `.badge` | `.badge-primary`, `.badge-secondary`, `.badge-sm`, `.badge-lg` |
| Avatar | `.avatar` | `.avatar.placeholder`, `.avatar-group` |
| Swap | `.swap` | `.swap-on`, `.swap-off`, `.swap-rotate` |
| Modal | `.modal` | `.modal-box`, `.modal-backdrop`, `.modal-action` |
| Stats | `.stats` | `.stat`, `.stat-title`, `.stat-value`, `.stat-figure` |
| Join | `.join` | `.join-item` |
| Indicator | `.indicator` | `.indicator-item` |
| Divider | `.divider` | - |
| Table | `.table` | `.table-zebra`, `.table-pin-rows` |

---

## 颜色系统

---

## 颜色系统

### 品牌色 (Brand Colors)

#### 主品牌色 - Kline (盼趣蓝)
```javascript
colors: {
  kline: {
    DEFAULT: '#002FA7',  // 主色
    50: '#E8EEFF',
    100: '#C4D4FF',
    200: '#9DB6FF',
    300: '#7599FF',
    400: '#4D7CFF',
    500: '#2560FF',
    600: '#002FA7',  // 主要使用
    700: '#002687',
    800: '#001D67',
    900: '#001447',
  }
}
```

**使用场景:**
- 主按钮: `bg-kline` 或 `background-color: #002FA7`
- 图标: `text-kline`
- 边框: `border-kline`
- 悬停效果: `hover:bg-kline hover:text-white`

#### 次要品牌色 - Brand (管理后台)
```javascript
colors: {
  brand: {
    50: '#E8EEFF',
    100: '#C4D4FF',
    200: '#9DB6FF',
    300: '#7599FF',
    400: '#4D7CFF',
    500: '#2560FF',
    600: '#002FA7',  // 主要使用
    700: '#002687',
    800: '#001D67',
    900: '#001447',
  }
}
```

**使用场景:**
- 强调文本: `text-brand-600`
- 卡片高亮: `text-brand-600` (金额、利润等)
- 徽章: `badge-brand-500`

### 渐变色

#### 主渐变文本
```css
.gradient-text {
  background: linear-gradient(135deg, #002FA7 0%, #2560FF 50%, #4D7CFF 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
```

**使用场景:**
- Logo 文字
- 页面标题
- 品牌名称

#### Hero 区域渐变背景
```css
.hero-gradient {
  background: linear-gradient(135deg, #0a0a0a 0%, #001447 50%, #0a0a0a 100%);
}
```

#### CTA 按钮渐变
```css
background: linear-gradient(135deg, #002FA7 0%, #2560FF 100%);
```

### 主题色

#### 亮色主题
- 背景色: `bg-base-100` (白色/浅色)
- 次级背景: `bg-base-200` (浅灰色)
- 文字色: `text-base-content` (深色)
- 文字透明度:
  - `text-base-content/60` - 次要文本
  - `text-base-content/70` - 辅助文本
  - `text-base-content/80` - 中等文本

#### 暗色主题
```css
[data-theme="dark"] {
  --text-primary: #f5f5f5;
  --text-secondary: #d4d4d4;
  --text-tertiary: #a3a3a3;
}
```

**使用场景:**
- 主要文本: `text-base-content` (映射到 `--text-primary`)
- 次要文本: `text-base-content/60` (映射到 `--text-secondary`)
- 辅助元素: `text-base-content/70`

### 功能色

- 成功: `text-success` / `#4ade80` (暗色模式)
- 警告: `text-warning`
- 错误: `text-error`
- 信息: `text-info`
- 强调: `text-accent`

---

## 字体系统

### 字体家族
```javascript
fontFamily: {
  sans: ['Inter', 'sans-serif'],
}
```

### 字体大小规范

| 用途 | Tailwind 类 | 大小 | 使用场景 |
|------|-------------|------|----------|
| 超大标题 | `text-5xl md:text-7xl` | 48px/80px | Hero 主标题 |
| 页面标题 | `text-3xl` | 30px | 标准页面标题 |
| 卡片标题 | `text-xl` / `text-lg` | 20px/18px | 卡片标题、区块标题 |
| 数字显示 | `text-2xl` | 24px | 金额、积分、统计数据 |
| 正文大 | `text-base` | 16px | 重要说明、描述文本 |
| 正文 | `text-sm` | 14px | 正文内容、表格内容 |
| 辅助文本 | `text-xs` | 12px | 标签、时间戳、次要说明 |

> **注意:** 为了保持标准 DaisyUI 的紧凑风格，v2 版本调整了部分字号：
> - 页面标题从 `text-4xl md:text-5xl` 改为 `text-3xl`
> - 数字显示从 `text-3xl md:text-4xl` 改为 `text-2xl`
> - 表格内容字号从 `0.875rem` 改为 `0.8125rem`

### 字重规范

| 用途 | Tailwind 类 | 字重 | 使用场景 |
|------|-------------|------|----------|
| 特轻 | `font-light` | 300 | 极少使用 |
| 常规 | `font-normal` | 400 | 正文 |
| 中等 | `font-medium` | 500 | 强调文本、导航链接 |
| 半粗 | `font-semibold` | 600 | 小标题、表头 |
| 粗体 | `font-bold` | 700 | 大标题、品牌名称 |

### 文本样式

#### 渐变文本
```html
<span class="gradient-text font-bold">盼趣AI</span>
```

#### 自动适配暗色模式文本
```css
/* 使用自定义类实现自动适配 */
[data-theme="dark"] .text-auto-dark {
  color: white;
}
[data-theme="light"] .text-auto-dark {
  color: inherit;
}
```

**使用示例:**
```html
<h2 class="text-4xl font-bold text-auto-dark">标题</h2>
<p class="text-lg text-auto-dark/70">描述文本</p>
```

---

## 布局系统

### 容器规范

#### 主容器
```html
<div class="container mx-auto px-4 py-8 max-w-7xl">
  <!-- 内容 -->
</div>
```

**说明:**
- `container` - 响应式容器
- `mx-auto` - 水平居中
- `px-4` - 左右内边距 (16px)
- `py-8` - 上下内边距 (32px)
- `max-w-7xl` - 最大宽度 1280px

### 网格系统

#### 响应式网格
```html
<!-- 4列网格 -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
  <!-- 卡片 -->
</div>

<!-- 3列网格 -->
<div class="grid grid-cols-1 md:grid-cols-3 gap-8">
  <!-- 卡片 -->
</div>

<!-- 2列网格 -->
<div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
  <!-- 内容 -->
</div>
```

**断点说明:**
- `sm`: 640px
- `md`: 768px
- `lg`: 1024px
- `xl`: 1280px
- `2xl`: 1536px

### 间距规范

| 用途 | Tailwind 类 | 大小 | 使用场景 |
|------|-------------|------|----------|
| 超小间距 | `gap-2` | 8px | 紧密排列的元素 |
| 小间距 | `gap-3` | 12px | 按钮组、图标+文字 |
| 中间距 | `gap-4` | 16px | 卡片网格、表单字段 |
| 大间距 | `gap-6` | 24px | 区块之间 |
| 超大间距 | `gap-8` | 32px | 大型区块之间 |
| 特大间距 | `gap-12` | 48px | 主要章节之间 |

### 内边距规范

| 用途 | Tailwind 类 | 大小 | 使用场景 |
|------|-------------|------|----------|
| 统计卡片 | `p-5` | 20px | 数据统计卡片 |
| 标准卡片 | `p-6` | 24px | 标准卡片 |
| 信息卡片 | `p-4` | 16px | 紧凑信息卡片 |
| 章节内边距 | `py-20` | 80px | 主要章节上下 |
| 页面内边距 | `px-4 py-8` | 16px/32px | 页面级内边距 |

> **v2 版本调整:** 统计卡片使用 `p-5`，信息卡片使用 `p-4` 以保持紧凑感

---

## 组件规范

### 导航栏 (Navbar)

#### 项目页面标准导航栏

**⚠️ 必须使用 DaisyUI navbar-start 和 navbar-end 结构**

```html
<div class="navbar navbar-with-sidebar sticky top-0 z-50 px-6 bg-base-100 shadow">
  <!-- Logo and Navigation - Start -->
  <div class="navbar-start">
    <div class="flex items-center gap-8">
      <!-- Logo -->
      <a href="index.html" class="flex items-center gap-3">
        <img src="https://panqu.com/assets/img/logo.webp" alt="盼趣AI" class="h-8">
        <span class="gradient-text font-bold text-xl">盼趣 AI</span>
      </a>

      <!-- Navigation Menu -->
      <ul class="menu menu-horizontal px-1 gap-2">
        <li>
          <a href="index.html">
            <i class="ri-dashboard-line"></i>
            <span>总览</span>
          </a>
        </li>
        <li>
          <a href="Projects.html" class="active">
            <i class="ri-folder-3-line"></i>
            <span>项目</span>
          </a>
        </li>
        <li>
          <a href="billing.html">
            <i class="ri-file-list-3-line"></i>
            <span>账单</span>
          </a>
        </li>
      </ul>
    </div>
  </div>

  <!-- Right side items - End -->
  <div class="navbar-end gap-2">
    <!-- Theme Toggle -->
    <label class="swap swap-rotate btn btn-ghost btn-circle">
      <input type="checkbox" id="theme-toggle" />
      <i class="ri-sun-line swap-on fill-current w-5 h-5 text-warning"></i>
      <i class="ri-moon-line swap-off fill-current w-5 h-5"></i>
    </label>

    <!-- Notification Bell -->
    <div class="dropdown dropdown-end">
      <div tabindex="0" role="button" class="btn btn-ghost btn-circle">
        <div class="indicator">
          <i class="ri-notification-3-line text-xl"></i>
          <span class="badge badge-sm badge-error indicator-item">3</span>
        </div>
      </div>
      <ul tabindex="0" class="dropdown-content z-[1] menu p-2 shadow bg-base-100 rounded-box w-80 border border-base-300">
        <!-- 通知内容 -->
      </ul>
    </div>

    <!-- User Dropdown -->
    <div class="dropdown dropdown-end">
      <div tabindex="0" role="button" class="btn btn-ghost btn-circle">
        <div class="avatar placeholder">
          <div class="bg-primary text-primary-content rounded-full w-10">
            <span class="text-sm font-bold">C</span>
          </div>
        </div>
      </div>
      <ul tabindex="0" class="menu menu-sm dropdown-content mt-3 z-[1] p-2 shadow bg-base-100 rounded-box w-52 border border-base-300">
        <!-- 用户菜单 -->
      </ul>
    </div>
  </div>
</div>
```

**关键要点:**
- ✅ **必须使用** `.navbar-start` 和 `.navbar-end`
- ✅ 导航菜单使用 `.menu.menu-horizontal`
- ✅ 菜单项内的图标和文字由 DaisyUI 自动布局
- ✅ 使用 `.dropdown.dropdown-end` 对齐下拉菜单
- ✅ 按钮使用标准类: `.btn.btn-ghost.btn-circle`
- ✅ 固定顶部: `sticky top-0`
- ✅ 高层: `z-50`

**避免的常见错误:**
- ❌ 使用 `flex-1` 和 `flex-none` 替代 `navbar-start` 和 `navbar-end`
- ❌ 在链接内添加 `flex items-center gap-2` (DaisyUI menu 组件会自动处理)
- ❌ 自定义按钮大小 (应使用 DaisyUI 的 `btn-sm`, `btn`, `btn-lg`)

#### 首页/营销页面导航栏

```html
<div class="navbar bg-base-100/80 backdrop-blur-md shadow-lg sticky top-0 z-50">
  <div class="navbar-start">
    <a href="#" class="btn btn-ghost text-xl">
      <img src="https://panqu.com/assets/img/logo.webp" alt="盼趣AI" class="h-8">
      <span class="gradient-text font-bold">盼趣AI</span>
    </a>
  </div>

  <div class="navbar-center hidden lg:flex">
    <ul class="menu menu-horizontal px-1">
      <li><a href="#" class="hover:text-kline transition-colors font-medium">首页</a></li>
      <li><a href="#" class="hover:text-kline transition-colors font-medium">功能</a></li>
    </ul>
  </div>

  <div class="navbar-end gap-3">
    <label class="swap swap-rotate btn btn-ghost btn-circle">
      <input type="checkbox" />
      <i class="ri-sun-line swap-on fill-current w-6 h-6 text-orange-500"></i>
      <i class="ri-moon-line swap-off fill-current w-6 h-6 text-kline"></i>
    </label>
    <a href="#" class="btn btn-outline btn-sm border-kline">登录</a>
    <a href="#" class="btn btn-sm bg-kline text-white">注册</a>
  </div>
</div>
```

### 按钮 (Buttons)

#### 主按钮
```html
<a href="#" class="btn btn-lg gap-2 hover:scale-105 transition-transform shadow-xl text-white border-0" style="background-color: #002FA7;">
  <i class="ri-movie-line text-xl"></i>
  立即制作
</a>
```

#### 次要按钮/轮廓按钮
```html
<a href="#" class="btn btn-outline btn-lg gap-2 hover:scale-105 transition-transform text-white border-white hover:bg-white hover:text-kline">
  <i class="ri-information-line text-xl"></i>
  了解更多
</a>
```

#### 小按钮
```html
<button class="btn btn-sm gap-2 hover:scale-105 transition-transform text-white border-0" style="background-color: #002FA7;">
  <i class="ri-volume-up-line"></i>
  体验音效
</button>
```

#### 图标按钮
```html
<button class="btn btn-circle btn-ghost hover:bg-kline hover:text-white transition-colors">
  <i class="ri-twitter-x-line text-xl"></i>
</button>
```

**按钮规范:**
- 尺寸: `btn-sm` / `btn` / `btn-lg`
- 悬停缩放: `hover:scale-105 transition-transform`
- 主按钮背景: `background-color: #002FA7`
- 阴影: `shadow-xl` (大按钮)
- 间距: `gap-2` (图标和文字之间)

### 卡片 (Cards)

#### 图片卡片
```html
<div class="card bg-base-100 shadow-xl card-hover transition-all duration-300 fade-in">
  <figure class="relative">
    <img src="..." alt="..." class="w-full h-48 object-cover" />
    <div class="absolute inset-0 bg-black/50 flex items-center justify-center opacity-0 hover:opacity-100 transition-opacity cursor-pointer">
      <button class="btn btn-circle btn-lg text-white border-0" style="background-color: #002FA7;">
        <i class="ri-play-fill text-3xl"></i>
      </button>
    </div>
  </figure>
  <div class="card-body">
    <h3 class="card-title">卡片标题</h3>
  </div>
</div>
```

#### 统计卡片（带图标）
```html
<div class="card bg-base-100 shadow-xl hover:shadow-2xl transition-all duration-300 fade-in">
  <div class="card-body p-5">
    <div class="text-base-content/60 text-sm mb-1 flex items-center gap-2">
      <i class="ri-money-dollar-circle-line text-base-content/50"></i>
      平台总收入
    </div>
    <div class="text-2xl font-bold text-base-content">¥1,250,000</div>
  </div>
</div>
```

**规范要求:**
- 卡片内边距: `p-5` (20px)
- 数字大小: `text-2xl` (24px)
- 标签文字: `text-sm` (14px)
- **必须包含图标**: 使用 `flex items-center gap-2` 将图标和文字组合
- **图标颜色**: `text-base-content/50` (半透明)
- **数字颜色**: 统一使用 `text-base-content`，特殊强调数据可使用 `text-brand-600`
- **常用图标**:
  - 收入/金额: `ri-money-dollar-circle-line`
  - 成本/支出: `ri-subtract-line` 或 `ri-money-dollar-circle-line`
  - 利润: `ri-line-chart-line`
  - 积分: `ri-coins-line`
  - 任务: `ri-task-line`
  - 用户: `ri-user-line`
  - 总消耗: `ri-funds-box-line`

#### 图标卡片
```html
<div class="card bg-base-200 shadow-xl card-hover transition-all duration-300 fade-in">
  <div class="card-body items-center text-center">
    <div class="text-5xl text-kline mb-4">
      <i class="ri-compass-line"></i>
    </div>
    <h4 class="card-title text-xl text-auto-dark">Explore</h4>
    <p class="text-sm text-auto-dark/70">描述文本</p>
  </div>
</div>
```

**卡片规范:**
- 基础样式: `bg-base-100 shadow-xl`
- 悬停效果: `hover:shadow-2xl card-hover`
- 动画: `transition-all duration-300 fade-in`
- 圆角: 默认 `rounded-xl` (DaisyUI)

### 统计卡片 (Stats)

#### 标准 Stats 组件

**⚠️ 必须使用 DaisyUI Stats 标准结构**

```html
<!-- 单个统计卡片 -->
<div class="stats shadow bg-base-100">
  <div class="stat">
    <div class="stat-figure text-primary">
      <i class="ri-folder-line text-3xl"></i>
    </div>
    <div class="stat-title">总项目数</div>
    <div class="stat-value text-primary">24</div>
  </div>
</div>

<!-- 多个统计项 -->
<div class="stats shadow bg-base-100">
  <div class="stat">
    <div class="stat-figure text-primary">
      <i class="ri-folder-line text-3xl"></i>
    </div>
    <div class="stat-title">总项目数</div>
    <div class="stat-value text-primary">24</div>
  </div>
  
  <div class="stat">
    <div class="stat-figure text-primary">
      <i class="ri-task-line text-3xl"></i>
    </div>
    <div class="stat-title">总任务次数</div>
    <div class="stat-value text-primary">12,450</div>
  </div>
</div>
```

**Stats 组件规范:**
- 容器: `.stats` (必须)
- 统计项: `.stat` (每个统计数据)
- 图标区: `.stat-figure` (可选，用于放置图标)
- 标题: `.stat-title` (描述文字)
- 数值: `.stat-value` (主要数据)
- 描述: `.stat-desc` (可选，补充说明)

**关键要点:**
- ✅ 使用 `.stats` 作为容器，而不是自定义 card
- ✅ 每个统计项使用 `.stat` 类
- ✅ 图标放在 `.stat-figure` 中
- ✅ 数值使用 `.stat-value` 类，自动获得大字体
- ✅ 可以使用 `text-primary` 等颜色类修饰
- ❌ 不要使用 `card` + 自定义布局替代 stats

### 搜索框和按钮组 (Join)

#### 搜索框组合

```html
<div class="join">
  <input type="search" id="projectSearch" placeholder="搜索项目..." 
         class="input input-bordered join-item w-full md:w-auto" />
  <button class="btn btn-neutral join-item">
    <i class="ri-search-line"></i>
  </button>
</div>
```

#### 分页按钮组

```html
<div class="join">
  <button class="join-item btn btn-sm" disabled>
    <i class="ri-arrow-left-s-line"></i>
  </button>
  <button class="join-item btn btn-sm btn-active">1</button>
  <button class="join-item btn btn-sm">2</button>
  <button class="join-item btn btn-sm">3</button>
  <button class="join-item btn btn-sm">
    <i class="ri-arrow-right-s-line"></i>
  </button>
</div>
```

**Join 组件规范:**
- 容器: `.join` (必须)
- 子元素: `.join-item` (每个连接的元素)
- 适用于: input、button、select 等表单元素
- 自动处理: 边框合并、圆角处理

**关键要点:**
- ✅ 使用 `.join` 将多个元素无缝连接
- ✅ 每个子元素必须添加 `.join-item`
- ✅ 可以混合不同类型的元素 (input + button)
- ✅ 激活状态使用 `.btn-active`
- ❌ 不要使用 flex 布局手动拼接元素

### 指示器 (Indicator)

#### 通知徽章

```html
<div class="dropdown dropdown-end">
  <div tabindex="0" role="button" class="btn btn-ghost btn-circle">
    <div class="indicator">
      <i class="ri-notification-3-line text-xl"></i>
      <span class="badge badge-sm badge-error indicator-item">3</span>
    </div>
  </div>
  <ul tabindex="0" class="dropdown-content z-[1] menu p-2 shadow bg-base-100 rounded-box w-80 border border-base-300">
    <!-- 通知内容 -->
  </ul>
</div>
```

**Indicator 组件规范:**
- 容器: `.indicator` (必须)
- 徽章: `.indicator-item` (显示在角落的元素)
- 位置: 默认右上角，可通过修饰类调整
- 常用组合: indicator + badge

**关键要点:**
- ✅ 使用 `.indicator` 包裹需要添加徽章的元素
- ✅ 徽章元素添加 `.indicator-item` 类
- ✅ 通常与 badge 组件配合使用
- ✅ 自动定位到容器的右上角
- ❌ 不要使用绝对定位手动放置徽章

### 分隔线 (Divider)

#### 标准分隔线

```html
<!-- 水平分隔线 -->
<div class="divider"></div>

<!-- 带文字的分隔线 -->
<div class="divider">或</div>

<!-- 垂直分隔线 -->
<div class="divider divider-horizontal"></div>

<!-- 无边距分隔线 (在菜单中) -->
<div class="divider my-0"></div>
```

**Divider 组件规范:**
- 基础类: `.divider`
- 垂直分隔: `.divider-horizontal`
- 可以包含文字内容
- 默认有上下边距，可用 `my-0` 移除

**关键要点:**
- ✅ 使用 DaisyUI 的 `.divider` 类
- ✅ 在下拉菜单中使用 `my-0` 移除边距
- ✅ 可以在 divider 内添加文字
- ❌ 不要使用 `<hr>` 或自定义边框线

### 徽章 (Badges)

#### 标准徽章
```html
<div class="badge badge-primary badge-lg mb-6 fade-in">
  <i class="ri-sparkling-fill mr-1"></i>
  全新升级 2.0
</div>

<div class="badge badge-secondary badge-lg mb-4">
  <i class="ri-sparkling-fill mr-1"></i>
  SOUND [BETA]
</div>
```

#### 菜单中的徽章
```html
<li><a class="justify-between">
  设置
  <span class="badge badge-sm badge-brand-500">New</span>
</a></li>
```

**徽章规范:**
- 尺寸: `badge-sm` / `badge` / `badge-lg`
- 颜色: `badge-primary` / `badge-secondary` / `badge-brand-500`
- 图标: 使用 RemixIcon, 间距 `mr-1`

### 表格 (Tables)

#### 标准表格
```html
<div class="overflow-x-auto bg-base-100 rounded-xl shadow-xl">
  <table class="table table-zebra table-pin-rows">
    <thead class="bg-base-200">
      <tr>
        <th>排名</th>
        <th>组织名称</th>
        <th>消费用户数</th>
        <th>消费积分</th>
        <th>充值金额(元)</th>
      </tr>
    </thead>
    <tbody id="orgTableBody">
      <!-- 表格行 -->
    </tbody>
  </table>
</div>
```

**表格规范:**
- 容器: `overflow-x-auto bg-base-100 rounded-xl shadow-xl`
- 表格类: `table table-zebra table-pin-rows`
- 表头: `bg-base-200`
- 单元格内边距: `0.75rem` 上下, `1rem` 左右 (在 shared/styles.css 中定义)
- 字体大小: `0.8125rem` (约 13px) (在 shared/styles.css 中定义)
- 最小行高: `2.75rem` (44px)

> **v2 版本调整:** 采用更紧凑的 DaisyUI 标准尺寸
> - padding 从 `1rem` 减小到 `0.75rem` (上下)
> - 字号从 `0.875rem` 减小到 `0.8125rem`
> - 最小行高从 `3.5rem` 减小到 `2.75rem`

### 下拉菜单 (Dropdowns)

#### 时间范围选择器
```html
<div class="dropdown dropdown-end">
  <div tabindex="0" role="button" class="btn btn-outline gap-2 w-full md:w-auto">
    <i class="ri-calendar-line text-lg"></i>
    <span id="dateRangeText">选择时间范围</span>
    <i class="ri-arrow-down-s-line"></i>
  </div>
  <ul tabindex="0" class="dropdown-content z-[1] menu p-2 shadow-xl bg-base-100 rounded-box w-52 border border-base-300">
    <li><a onclick="setDateRange('today', '今日')">今日</a></li>
    <li><a onclick="setDateRange('week', '本周')">本周</a></li>
    <li><a onclick="setDateRange('month', '本月')">本月</a></li>
    <div class="divider my-0"></div>
    <li>
      <a class="flex flex-col gap-2">
        <span class="text-sm font-semibold">自定义区间</span>
        <input type="date" class="input input-bordered input-sm w-full" id="startDate" />
        <span class="text-xs text-center">至</span>
        <input type="date" class="input input-bordered input-sm w-full" id="endDate" />
      </a>
    </li>
  </ul>
</div>
```

**下拉菜单规范:**
- 容器: `dropdown dropdown-end`
- 触发器: `tabindex="0" role="button"`
- 菜单: `dropdown-content z-[1] menu p-2 shadow-xl bg-base-100 rounded-box`
- 宽度: `w-52` (标准) / `w-40` (小)
- 分隔线: `<div class="divider my-0"></div>`

### 分页 (Pagination)

#### 标准分页
```html
<div class="flex flex-col md:flex-row justify-between items-center gap-4 px-6 py-4 bg-base-200/50 mt-4 rounded-xl">
  <div class="text-sm text-base-content/70">
    显示第 <span id="orgPaginationStart" class="font-bold text-base-content">1</span>
    到 <span id="orgPaginationEnd" class="font-bold text-base-content">10</span>
    条，共 <span id="orgPaginationTotal" class="font-bold text-base-content">0</span> 条记录
  </div>

  <div class="join">
    <button class="join-item btn btn-sm">
      <i class="ri-skip-back-line"></i>
    </button>
    <button class="join-item btn btn-sm">
      <i class="ri-arrow-left-s-line"></i>
    </button>
    <div class="join">
      <!-- 页码 -->
    </div>
    <button class="join-item btn btn-sm">
      <i class="ri-arrow-right-s-line"></i>
    </button>
    <button class="join-item btn btn-sm">
      <i class="ri-skip-forward-line"></i>
    </button>
  </div>

  <div class="flex items-center gap-2">
    <span class="text-sm text-base-content/70">每页显示</span>
    <select class="select select-bordered select-sm w-20">
      <option value="10">10</option>
      <option value="20">20</option>
      <option value="50">50</option>
    </select>
    <span class="text-sm text-base-content/70">条</span>
  </div>
</div>
```

**分页规范:**
- 容器: 响应式 flex 布局
- 按钮组: `join` 类
- 图标: RemixIcon 翻页图标
- 选择器: `select select-bordered select-sm`

### Logo 规范

#### Logo 图片资源
- **亮色主题 Logo**: `https://panqu.com/assets/img/logo.webp`
- **暗色主题 Logo**: `https://panqu.com/assets/img/logo-white.webp`

#### Logo 使用规范

##### 导航栏 Logo (带文字)
```html
<!-- 首页导航栏 -->
<a href="#" class="btn btn-ghost text-xl">
  <img src="https://panqu.com/assets/img/logo.webp" alt="盼趣AI" class="h-8">
  <span class="gradient-text font-bold">盼趣AI</span>
</a>

<!-- 侧边栏 Logo (管理后台) -->
<div class="sidebar-logo">
  <a href="index.html">
    <i class="ri-robot-2-fill text-brand-600 text-3xl"></i>
    <span class="gradient-text font-bold text-xl">盼趣 AI</span>
  </a>
</div>
```

**说明:**
- Logo 高度: `h-8` (32px)
- 保持 logo 图片和渐变文字同时显示
- 渐变文字始终使用 `.gradient-text` 类
- 侧边栏可使用图标+文字组合

##### 仅 Logo 图片
```html
<!-- 不带文字的纯 logo -->
<a href="#" class="btn btn-ghost text-xl">
  <img src="https://panqu.com/assets/img/logo.webp" alt="盼趣AI" class="h-8">
</a>
```

### 主题切换 (Swap)

#### 标准主题切换按钮
```html
<label class="swap swap-rotate btn btn-ghost btn-circle">
  <input type="checkbox" id="theme-toggle" />
  <i class="ri-sun-line swap-on fill-current w-5 h-5 text-warning"></i>
  <i class="ri-moon-line swap-off fill-current w-5 h-5"></i>
</label>
```

**Swap 组件规范:**
- 容器: `swap swap-rotate` (旋转效果)
- 触发器: `input type="checkbox"` (必须)
- 显示元素: `.swap-on` (选中时显示) / `.swap-off` (未选中时显示)
- 图标尺寸: `w-5 h-5` (20px) 或 `w-6 h-6` (24px)

**重要说明:**
- ✅ 使用 RemixIcon 的 `<i>` 标签时,需要添加 flex 居中样式:
```css
.navbar-with-sidebar .swap i {
  display: flex;
  align-items: center;
  justify-content: center;
}
```
- ✅ 或者使用 DaisyUI 官方推荐的 `<svg>` 图标(不需要额外样式)
- ❌ 不要在 swap 容器上使用 `text-center` 类

### 弹窗 (Modal)

#### HTML Dialog Modal (推荐)
```html
<dialog id="my_modal_1" class="modal">
  <div class="modal-box">
    <form method="dialog">
      <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2">✕</button>
    </form>
    <h3 class="font-bold text-lg">Modal 标题</h3>
    <p class="py-4">Modal 内容描述</p>
    <div class="modal-action">
      <form method="dialog">
        <button class="btn">取消</button>
      </form>
      <button class="btn btn-primary">确认</button>
    </div>
  </div>
  <form method="dialog" class="modal-backdrop">
    <button>close</button>
  </form>
</dialog>
```

#### 删除确认弹窗示例
```html
<dialog id="delete_confirm_modal" class="modal">
  <div class="modal-box">
    <form method="dialog">
      <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2">✕</button>
    </form>
    <h3 class="font-bold text-lg">确认删除项目</h3>
    <p class="py-4" id="deleteProjectName">项目名称</p>
    <p class="text-sm text-base-content/60 mb-4">
      <i class="ri-error-warning-line text-error mr-1"></i>
      此操作不可撤销,删除后项目将无法恢复。
    </p>
    <div class="modal-action">
      <form method="dialog">
        <button class="btn btn-outline">取消</button>
      </form>
      <button class="btn btn-error" onclick="confirmDelete()">
        <i class="ri-delete-bin-line mr-1"></i>
        确认删除
      </button>
    </div>
  </div>
  <form method="dialog" class="modal-backdrop">
    <button>close</button>
  </form>
</dialog>
```

**Modal 组件规范:**
- 使用 HTML5 `<dialog>` 元素 (推荐,支持 ESC 关闭)
- 容器: `dialog.modal`
- 内容区: `div.modal-box`
- 关闭按钮: `form method="dialog"` + `button` (自动关闭)
- 背景点击关闭: `form.modal-backdrop`
- 标题: `h3.font-bold.text-lg`
- 内容: `p.py-4` (标准垂直间距)
- 操作区: `div.modal-action` (按钮容器)

**JavaScript 控制:**
```javascript
// 打开 Modal
document.getElementById('my_modal_1').showModal();

// 关闭 Modal
document.getElementById('my_modal_1').close();
```

**避免的常见错误:**
- ❌ 在 modal-box 上添加 `text-center` (破坏默认布局)
- ❌ 在 modal-action 上添加 `justify-center` (按钮会自动靠右对齐)
- ❌ 自定义标题样式,应使用 `font-bold text-lg`

### 头像 (Avatar)

#### Avatar Placeholder (文字头像)
```html
<div class="avatar placeholder">
  <div class="bg-primary text-primary-content rounded-full w-10">
    <span class="text-sm font-bold">C</span>
  </div>
</div>
```

#### 按钮内的头像
```html
<div class="btn btn-ghost btn-circle">
  <div class="avatar placeholder">
    <div class="bg-primary text-primary-content rounded-full w-10">
      <span class="text-sm font-bold">C</span>
    </div>
  </div>
</div>
```

**Avatar 组件规范:**
- 容器: `.avatar.placeholder`
- 内容区: `div.bg-primary.text-primary-content.rounded-full`
- 尺寸: `w-8` (小) / `w-10` (标准) / `w-12` (大)
- 文字: `span.text-sm.font-bold` (w-10 使用 text-sm)
- 颜色: `bg-primary` + `text-primary-content`

**重要说明:**
- ✅ avatar 和 placeholder 类是分开的
- ✅ 使用嵌套结构: `avatar.placeholder` > `div` > `span`
- ✅ 内部 div 必须指定宽度和圆角
- ❌ 不要把 `avatar placeholder` 放在 `btn` 上,应该作为子元素

### 页脚 (Footer)

#### 标准页脚
```html
<footer class="footer footer-center p-10 bg-base-200 text-base-content">
  <div class="grid grid-cols-1 md:grid-cols-4 gap-8 w-full max-w-7xl">
    <div class="text-center md:text-left">
      <div class="flex items-center justify-center md:justify-start gap-2 mb-4">
        <img src="https://panqu.com/assets/img/logo.webp" alt="盼趣AI" class="h-10">
        <span class="text-2xl font-bold gradient-text">盼趣AI</span>
      </div>
      <p class="text-base-content/70 mb-4">描述文本</p>
      <div class="flex gap-4 justify-center md:justify-start">
        <a href="#" class="btn btn-circle btn-ghost hover:bg-kline hover:text-white transition-colors">
          <i class="ri-twitter-x-line text-xl"></i>
        </a>
      </div>
    </div>

    <div>
      <h4 class="footer-title font-bold text-lg mb-4">Product</h4>
      <ul class="space-y-2">
        <li><a href="#" class="link link-hover hover:text-kline">Features</a></li>
        <li><a href="#" class="link link-hover hover:text-kline">Pricing</a></li>
      </ul>
    </div>
  </div>

  <div class="divider"></div>

  <div class="text-center">
    <p class="text-base-content/60">&copy; 2025 Panqu AI. All rights reserved.</p>
  </div>
</footer>
```

---

## 动画效果

### 淡入动画 (Fade In)

#### CSS 定义
```css
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}
.fade-in { animation: fadeIn 0.5s ease-out; }
```

**使用场景:**
- 页面加载动画
- 卡片进入动画
- 列表项显示

**使用示例:**
```html
<div class="card fade-in">
  <!-- 内容 -->
</div>
```

### 悬停效果

#### 卡片悬停
```html
<div class="card card-hover hover:scale-105 transition-all duration-300">
  <!-- 内容 -->
</div>
```

```css
.card-hover:hover {
  transform: translateY(-8px);
  box-shadow: 0 25px 50px -12px rgba(0, 47, 167, 0.25);
}
```

#### 按钮悬停
```html
<button class="btn hover:scale-105 transition-transform">
  按钮
</button>
```

#### 图标按钮悬停
```html
<button class="btn btn-circle hover:bg-kline hover:text-white transition-colors">
  <i class="ri-twitter-x-line text-xl"></i>
</button>
```

### 过渡效果规范

| 效果 | Tailwind 类 | 持续时间 | 使用场景 |
|------|-------------|----------|----------|
| 快速 | `transition-all duration-150` | 150ms | 小元素 |
| 标准 | `transition-all duration-300` | 300ms | 卡片、按钮 |
| 缓慢 | `transition-all duration-500` | 500ms | 大型区块 |
| 缓动 | `ease-out` | - | 淡入动画 |
| 颜色过渡 | `transition-colors` | - | 文字、背景色 |

---

## 主题切换

### HTML 结构
```html
<html lang="zh-CN" data-theme="dark">
  <!-- 或 -->
  <html lang="zh-CN" data-theme="light">
</html>
```

### 主题切换按钮
```html
<label class="swap swap-rotate btn btn-ghost btn-circle">
  <input type="checkbox" id="theme-toggle" />
  <i class="ri-sun-line swap-on fill-current w-6 h-6 text-orange-500"></i>
  <i class="ri-moon-line swap-off fill-current w-6 h-6 text-kline"></i>
</label>
```

### JavaScript 实现
```javascript
const themeToggle = document.getElementById('theme-toggle');
const html = document.documentElement;

themeToggle.addEventListener('change', () => {
  if (themeToggle.checked) {
    html.setAttribute('data-theme', 'light');
  } else {
    html.setAttribute('data-theme', 'dark');
  }
});
```

### Tailwind 配置
```javascript
tailwind.config = {
  darkMode: 'class', // 使用 class 模式
  // ...
}
```

### 暗色模式样式优化 (v2.2.0 更新)

在 `billing-common.css` 中定义了全面的暗黑模式文字颜色优化:

**核心原则: 所有文字在暗黑模式下必须是白色或接近白色，确保最佳可读性**

```css
/* 主要文字 - 纯白色 */
[data-theme="dark"] .text-base-content {
    color: #ffffff !important;
}

/* 次要文字 - 95% 白色 */
[data-theme="dark"] .text-base-content/60 {
    color: rgba(255, 255, 255, 0.95) !important;
}

/* 辅助文字 - 95% 白色 */
[data-theme="dark"] .text-base-content/70 {
    color: rgba(255, 255, 255, 0.95) !important;
}

/* 其他透明度文字 - 90% 白色 */
[data-theme="dark"] .text-base-content/50 {
    color: rgba(255, 255, 255, 0.9) !important;
}

[data-theme="dark"] .text-base-content/80 {
    color: #ffffff !important;
}
```

**全面的暗黑模式文字颜色优化:**

```css
/* 卡片标题 */
[data-theme="dark"] .card-title {
    color: #ffffff !important;
}

/* 菜单项 */
[data-theme="dark"] .menu li a {
    color: #ffffff !important;
}

/* 表格内容 */
[data-theme="dark"] table th,
[data-theme="dark"] table td {
    color: #ffffff !important;
}

/* 按钮文字 */
[data-theme="dark"] .btn {
    color: #ffffff !important;
}

/* 输入框和下拉框 */
[data-theme="dark"] select,
[data-theme="dark"] input {
    color: #ffffff !important;
}

/* 徽章文字 */
[data-theme="dark"] .badge {
    color: #ffffff !important;
}

/* 下拉菜单内容 */
[data-theme="dark"] .dropdown-content {
    color: #ffffff !important;
}

/* 所有图标 - 90% 白色 */
[data-theme="dark"] i {
    color: rgba(255, 255, 255, 0.9) !important;
}
```

**品牌色在暗黑模式下的优化:**

```css
/* 品牌蓝色 - 使用更亮的色调提升对比度 */
[data-theme="dark"] .text-brand-600 {
    color: #6366f1 !important;
}

[data-theme="dark"] .text-brand-500 {
    color: #818cf8 !important;
}

[data-theme="dark"] .badge-brand-500 {
    background-color: #6366f1 !important;
    color: #ffffff !important;
}
```

**重要说明:**
- 所有 `!important` 确保样式优先级最高
- 使用 `rgba()` 设置透明度可以保持文字层次感
- 暗黑模式下必须避免使用灰色文字，必须使用白色

---

## 响应式设计

### 断点系统

| 断点 | 屏幕宽度 | 使用场景 |
|------|----------|----------|
| `default` | < 640px | 移动设备 |
| `sm` | ≥ 640px | 大屏手机 |
| `md` | ≥ 768px | 平板设备 |
| `lg` | ≥ 1024px | 小屏笔记本 |
| `xl` | ≥ 1280px | 桌面设备 |
| `2xl` | ≥ 1536px | 大屏显示器 |

### 响应式布局示例

#### 导航栏
```html
<ul class="menu menu-horizontal px-1 gap-2 hidden md:flex">
  <!-- 在 md 及以上显示 -->
</ul>
```

#### 网格布局
```html
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
  <!-- 移动: 1列, 平板: 2列, 桌面: 4列 -->
</div>
```

#### 标题大小
```html
<h1 class="text-4xl md:text-5xl lg:text-6xl">
  <!-- 移动: 36px, 平板: 48px, 桌面: 60px -->
</h1>
```

#### 按钮布局
```html
<div class="flex flex-col sm:flex-row gap-4">
  <!-- 移动: 垂直排列, 桌面: 水平排列 -->
</div>
```

### 移动端优化

#### 隐藏/显示
```html
<div class="hidden md:block">在移动端隐藏</div>
<div class="block md:hidden">仅在移动端显示</div>
```

#### 触摸友好的按钮
```html
<button class="btn btn-circle">
  <!-- 至少 44x44px -->
</button>
```

---

## 代码组织

### HTML 文件结构

```html
<!DOCTYPE html>
<html lang="zh-CN" data-theme="dark">
<head>
  <!-- 1. Meta 标签 -->
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>页面标题</title>
  <meta name="description" content="页面描述">

  <!-- 2. 字体 -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <!-- 3. 图标 -->
  <link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">

  <!-- 4. CSS 框架 -->
  <link href="https://cdn.jsdelivr.net/npm/daisyui@4.6.0/dist/full.min.css" rel="stylesheet" />
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- 5. Tailwind 配置 -->
  <script>
    tailwind.config = {
      darkMode: 'class',
      theme: {
        extend: {
          fontFamily: {
            sans: ['Inter', 'sans-serif'],
          },
          colors: {
            kline: {
              // ...
            }
          }
        }
      }
    }
  </script>

  <!-- 6. 自定义样式 -->
  <style>
    /* 最少自定义 CSS */
  </style>
</head>

<body class="min-h-screen bg-base-100">
  <!-- 页面内容 -->

  <!-- JavaScript -->
  <script>
    // 页面逻辑
  </script>
</body>
</html>
```

### 共享样式文件

**位置:** `billing_demo/v2/shared/styles.css`

**内容:**
1. 动画定义 (fadeIn)
2. 渐变文本样式
3. 暗色模式文字优化
4. 表格统一样式

**使用方法:**
```html
<link rel="stylesheet" href="shared/styles.css">
```

### CSS 规范

#### 优先使用 Tailwind 工具类
```html
<!-- 推荐 -->
<div class="flex items-center gap-4 p-6 bg-white rounded-xl shadow-lg">

<!-- 不推荐 -->
<div class="custom-card">
```

#### 自定义 CSS 的最小化
仅在以下情况使用自定义 CSS:
1. Tailwind 无法实现的动画
2. 复杂的渐变效果
3. 暗色模式的特殊优化
4. 第三方组件的样式覆盖

#### 自定义样式组织
```css
/* ============================================ */
/* 动画效果                                     */
/* ============================================ */

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

/* ============================================ */
/* 组件样式                                     */
/* ============================================ */

.gradient-text {
  /* ... */
}

/* ============================================ */
/* 主题优化                                     */
/* ============================================ */

[data-theme="dark"] {
  /* ... */
}
```

### JavaScript 规范

#### 使用现代 ES6+ 语法
```javascript
// 推荐
const elements = document.querySelectorAll('.card');
const handleClick = () => { /* ... */ };

// 不推荐
var elements = document.getElementsByClassName('card');
function handleClick() { /* ... */ }
```

#### 事件委托
```javascript
// 推荐
document.getElementById('parent').addEventListener('click', (e) => {
  if (e.target.matches('.btn-action')) {
    // 处理点击
  }
});
```

#### 主题切换逻辑
```javascript
const themeToggle = document.getElementById('theme-toggle');
const html = document.documentElement;

// 初始化
const savedTheme = localStorage.getItem('theme') || 'dark';
html.setAttribute('data-theme', savedTheme);
themeToggle.checked = savedTheme === 'light';

// 切换事件
themeToggle.addEventListener('change', () => {
  const newTheme = themeToggle.checked ? 'light' : 'dark';
  html.setAttribute('data-theme', newTheme);
  localStorage.setItem('theme', newTheme);
});
```

---

## 最佳实践

### 1. 性能优化

#### 图片优化
```html
<!-- 使用适当的尺寸和格式 -->
<img src="..." alt="..." class="w-full h-48 object-cover" loading="lazy">
```

#### 字体加载
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
```

### 2. 可访问性

#### 语义化 HTML
```html
<nav class="navbar">...</nav>
<main class="container">...</main>
<footer class="footer">...</footer>
```

#### Alt 文本
```html
<img src="..." alt="描述性文本">
```

#### 按钮标签
```html
<button aria-label="关闭">
  <i class="ri-close-line"></i>
</button>
```

### 3. SEO 优化

#### Meta 标签
```html
<meta name="description" content="页面描述">
<title>页面标题 | 盼趣 AI</title>
```

#### 结构化数据
```html
<h1>主标题</h1>
<h2>章节标题</h2>
<h3>小节标题</h3>
```

### 4. 浏览器兼容性

#### CDN 备份
```html
<script src="https://cdn.bootcdn.net/ajax/libs/Chart.js/4.4.1/chart.umd.min.js"
        onerror="this.onerror=null; this.src='https://unpkg.com/chart.js@4.4.1/dist/chart.umd.min.js'"></script>
```

#### 自动前缀
Tailwind CSS 自动添加浏览器前缀

### 5. 开发工作流

#### 组件复用
- 将常用组件保存为代码片段
- 使用共享样式文件
- 保持类名一致性

#### 命名规范
- ID: 使用 kebab-case (`date-range-text`)
- Class: 使用 Tailwind 工具类组合
- 自定义类: 使用 kebab-case (`.gradient-text`)

---

## 快速参考

### 常用类组合

#### 页面标题区 (v2 标准尺寸)
```html
<div class="flex flex-col md:flex-row justify-between items-start md:items-center mb-8 gap-4 fade-in">
  <div>
    <h1 class="text-3xl font-bold text-base-content mb-2">页面标题</h1>
    <p class="text-base-content/60 text-base">页面描述</p>
  </div>
  <!-- 操作按钮 -->
</div>
```

> **尺寸规范:**
> - 标题大小: `text-3xl` (30px) - 使用固定尺寸，不响应式放大
> - 描述文字: `text-base` (16px) - 从 `text-lg` 调整下来
> - 间距: `mb-2` (8px) - 标题和描述之间
> - 容器间距: `mb-8` (32px) - 与下一区块

#### 标准卡片
```html
<div class="card bg-base-100 shadow-xl hover:shadow-2xl transition-all duration-300 fade-in">
  <div class="card-body">
    <h3 class="card-title">卡片标题</h3>
    <p class="text-base-content/70">卡片内容</p>
  </div>
</div>
```

#### 按钮 + 图标
```html
<button class="btn gap-2 hover:scale-105 transition-transform text-white border-0" style="background-color: #002FA7;">
  <i class="ri-rocket-line"></i>
  按钮文字
</button>
```

### 颜色速查

| 用途 | 类名/值 | 颜色 |
|------|---------|------|
| 主品牌色 | `text-kline` / `bg-kline` / `#002FA7` | 深蓝色 |
| 渐变文本 | `.gradient-text` | 蓝色渐变 |
| 主背景 | `bg-base-100` | 白/深色 |
| 次背景 | `bg-base-200` | 浅灰/深灰 |
| 主要文字 | `text-base-content` | 深色/浅色 |
| 次要文字 | `text-base-content/60` | 半透明 |

### 图标速查 (RemixIcon)

| 图标 | 类名 | 使用场景 |
|------|------|----------|
| 🤖 | `ri-robot-2-fill` | Logo (侧边栏，可选) |
| 🏠 | `ri-home-line` | 首页 |
| ⚙️ | `ri-settings-line` | 设置 |
| 📅 | `ri-calendar-line` | 日期 |
| 🔔 | `ri-notification-line` | 通知 |
| 👤 | `ri-user-line` | 用户 |
| 🌙 | `ri-moon-line` | 暗色模式 |
| ☀️ | `ri-sun-line` | 亮色模式 |
| ▶️ | `ri-play-fill` | 播放 |
| ⬇️ | `ri-arrow-down-s-line` | 下拉 |
| ➕ | `ri-add-line` | 添加 |
| ❌ | `ri-close-line` | 关闭 |
| ✅ | `ri-check-line` | 确认 |
| 🔍 | `ri-search-line` | 搜索 |
| 📊 | `ri-bar-chart-line` | 图表 |
| 💰 | `ri-money-dollar-circle-line` | 账单 |
| 📝 | `ri-file-list-line` | 列表 |
| ⚡ | `ri-flashlight-line` | 快速 |
| 🎨 | `ri-palette-line` | 样式 |
| 🔧 | `ri-tools-line` | 工具 |

---

## 更新日志

### 2026-01-20 (v3.1.0)
- **DaisyUI 规范调整与优化**
  - 明确自定义类的使用规范: `navbar-with-sidebar` 和 `gradient-text`
  - 新增自定义类必须在独立 CSS 文件中定义的要求
  - 优化项目页面导航栏结构，确保完全符合 DaisyUI 标准
  - 规范化 Stats 组件的使用方式

- **组件规范完善**
  - 新增 Stats 组件标准结构和使用规范
  - 新增 Join 组件规范（用于搜索框和分页）
  - 新增 Indicator 组件规范（用于通知徽章）
  - 完善 Form Control 组件规范

- **项目结构优化**
  - 要求所有自定义样式必须放在 `assets/css/` 目录下
  - 规范化 Tailwind 配置文件的位置和命名
  - 明确项目特定样式文件的组织方式

- **最佳实践更新**
  - 强调 95% 使用 DaisyUI 标准类，仅 5% 使用自定义类
  - 自定义类仅用于: 渐变效果、特殊动画、项目特定布局增强
  - 禁止覆盖 DaisyUI 核心组件样式

### 2025-01-20 (v3.0.1)
- **组件规范完善**
  - 新增 Swap 组件规范(主题切换按钮)
  - 新增 Modal 组件规范(HTML Dialog 方式)
  - 更新 Avatar 组件规范(修正用户头像示例结构)
  - 添加 RemixIcon 图标居中问题解决方案

- **HTML 结构标准化**
  - Modal 使用 `<dialog>` 元素和标准结构
  - Avatar 使用正确的嵌套结构: `avatar.placeholder` > `div` > `span`
  - Swap 组件图标居中: 添加 flex 布局到 `<i>` 标签
  - 移除不必要的 text-center 和自定义布局类

- **CSS 优化**
  - 项目样式文件添加 swap 图标居中样式
  - 强调遵循 DaisyUI 官方文档标准
  - 避免覆盖 DaisyUI 默认样式

### 2025-01-20 (v3.0.0)
- **DaisyUI 标准化重大更新**
  - 新增 "DaisyUI 核心原则" 章节，强调必须严格遵守 DaisyUI 标准
  - 明确要求使用 navbar-start/navbar-end 替代自定义 flex 布局
  - 规范化所有组件的 HTML 结构，必须符合 DaisyUI 标准
  - 强调不要在自定义 CSS 中覆盖 DaisyUI 样式
  - 要求使用 DaisyUI 变量而非硬编码颜色

- **项目页面导航栏规范**
  - 添加项目页面标准导航栏结构
  - 规范化导航菜单使用 `.menu.menu-horizontal`
  - 明确菜单项内的图标和文字由 DaisyUI 自动处理
  - 添加 navbar-end 按钮组规范 (主题切换、通知、用户头像)
  - 统一使用 `dropdown.dropdown-end` 对齐下拉菜单

- **组件结构标准化**
  - Card 组件必须使用标准结构: `.card` > `figure` + `.card-body`
  - Dropdown 组件标准结构: `div.dropdown` > `div[tabindex]` + `ul.dropdown-content`
  - Menu 组件标准结构: `ul.menu` > `li` > `a`
  - Modal 组件标准结构: `dialog.modal` > `div.modal-box` + `form.modal-backdrop`

- **常见错误示例**
  - ❌ 使用 `flex-1/flex-none` 替代 `navbar-start/navbar-end`
  - ❌ 在 menu 链接内添加 `flex items-center gap-2`
  - ❌ 自定义按钮大小，应使用 DaisyUI 的 `btn-sm/btn/btn-lg`
  - ❌ 硬编码样式，应使用 DaisyUI 主题变量

### 2025-01-18 (v2.2.0)
- **暗黑模式文字优化**
  - 所有文字在暗黑模式下统一使用白色或接近白色 (90-95% 白色)
  - 提升次要文字透明度: `/60` 从 70% 提升到 95%, `/70` 提升到 95%
  - 优化品牌色在暗黑模式下的对比度
  - 新增全面的暗黑模式文字颜色优化规则

- **页面结构统一**
  - 统一所有账单页面的 HTML 结构和缩进
  - 统一图表容器高度为 `h-64` (256px)
  - 统一统计卡片使用 `text-base-content` 颜色
  - 统一 z-index 为 `z-[1]`
  - 添加图标到所有统计卡片

- **组件规范更新**
  - 统计卡片必须包含图标: `flex items-center gap-2`
  - 图标颜色使用 `text-base-content/50`
  - 标准化下拉菜单 z-index 和样式

### 2025-01-18 (v2.1.0)
- **Logo 规范更新**
  - 添加品牌 logo 图片资源规范
  - 更新导航栏和页脚的 logo 使用方式
  - 支持 logo 图片 + 渐变文字组合
  - 保留侧边栏的图标 + 文字选项
  - 统一 logo 高度为 `h-8` (32px)

### 2025-01-10 (v2.0.0)
- **尺寸标准化调整**
  - 优化表格单元格尺寸: padding 从 1rem 减小到 0.75rem,字号从 0.875rem 减小到 0.8125rem
  - 调整页面标题: 从 `text-4xl md:text-5xl` 改为 `text-3xl` (固定尺寸)
  - 优化数字显示: 统一使用 `text-2xl` 替代 `text-3xl md:text-4xl`
  - 卡片内边距微调: 统计卡片使用 `p-5`,信息卡片使用 `p-4`
  - 更新所有相关组件规范和示例代码
  - 统一为标准 DaisyUI 的紧凑风格

### 2025-01-09 (v1.0.0)
- 初始版本
- 基于 index.html 和 admin-dashboard.html 生成
- 包含完整的颜色、字体、布局、组件规范
- 添加动画效果和主题切换规范
- 添加响应式设计和最佳实践

---

## 维护说明

### 规范更新流程
1. 新增组件时更新对应章节
2. 颜色/字体变更需更新 Tailwind 配置
3. 定期审查和优化代码示例
4. 保持与实际代码库同步

### DaisyUI 合规检查清单
在提交代码前，确保：
- [ ] 使用了 DaisyUI 标准组件类 (navbar, card, dropdown, menu 等)
- [ ] 遵循了 DaisyUI 标准 HTML 结构
- [ ] 没有在自定义 CSS 中覆盖 DaisyUI 样式
- [ ] 使用了 DaisyUI 语义化类 (navbar-start, navbar-end, card-body 等)
- [ ] 避免硬编码颜色，使用 DaisyUI 主题变量

### 反馈机制
- 发现不符合 DaisyUI 标准的代码时及时更新
- 团队评审后方可修改核心规范
- 重大变更需更新版本号

---

**文档版本:** v3.1.0
**最后更新:** 2026-01-20
**维护者:** 盼趣 AI 前端团队
